apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: aapexternaldb
spec:
  destination:
    namespace: automation-platform-external-db
    server: https://kubernetes.default.svc
  source:
    path: aap
    repoURL: https://trrnas.texasroadracing.com:3100/TexasRoadracing/openshift
    targetRevision: externaldb
  sources: []
  project: default
  syncPolicy:
    syncOptions:
      - CreateNamespace=true
      - ApplyOutOfSyncOnly=true
project: default
source:
  repoURL: https://trrnas.texasroadracing.com:3100/TexasRoadracing/openshift.git
  path: aap/
  targetRevision: externaldb
destination:
  server: https://kubernetes.default.svc
  namespace: automation-platform-external-db
syncPolicy:
  automated:
    selfHeal: true
    enabled: true
  syncOptions:
    - CreateNamespace=true
    - ApplyOutOfSyncOnly=true
